name: Dummy run

on:
  workflow_dispatch:
  repository_dispatch:
    types: [dummy-ping]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Run dummy script
        run: python hello.py
